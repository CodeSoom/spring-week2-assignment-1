import axios from 'axios';

class {
  onCreate() {
    this.state = {
      task: [],
    }

    this.loadTasks();
  }

  async loadTasks() {
    const { data } = await axios.get('http://localhost.com:8080/tasks')
  }
}

<h1>ToDo</h1>
<ol>
  <for|task| of=tasks>
    <li>
      ${task.title}
    </li>
  </for>
</ol>
